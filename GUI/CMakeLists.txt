PROJECT(PVGUI)

SET(VTK_TCL_HOME ${PVGUI_SOURCE_DIR})
INCLUDE_DIRECTORIES(
  ${TCL_INCLUDE_PATH} ${TK_INCLUDE_PATH}
  ${KWCommon_SOURCE_DIR}
  )

SUBDIRS(Widgets)

SUBDIRS(Client)

IF(BUILD_TESTING)
  SET (PARAVIEW_EXECUTABLE ${EXECUTABLE_OUTPUT_PATH}/paraview)
  SET(PARAVIEW_CS_EXECUTABLE ${EXECUTABLE_OUTPUT_PATH}/TestClientServer)
  SUBDIRS(Testing)
ENDIF(BUILD_TESTING)
