IF(VTK_ZLIB_INCLUDE_DIRS)
  INCLUDE_DIRECTORIES(${VTK_ZLIB_INCLUDE_DIRS})
ELSE(VTK_ZLIB_INCLUDE_DIRS)
  IF(VTK_USE_SYSTEM_ZLIB)
    INCLUDE_DIRECTORIES(${ZLIB_INCLUDE_DIR})
  ELSE(VTK_USE_SYSTEM_ZLIB)
    INCLUDE_DIRECTORIES(${VTK_SOURCE_DIR}/Utilities 
                        ${VTK_BINARY_DIR}/Utilities)
  ENDIF(VTK_USE_SYSTEM_ZLIB)
ENDIF(VTK_ZLIB_INCLUDE_DIRS)

ADD_EXECUTABLE(KWConvertImageToHeader KWConvertImageToHeader.cxx)

TARGET_LINK_LIBRARIES(KWConvertImageToHeader
  vtkCommon
  vtkFiltering
  vtkImaging
  vtkIO
  )
