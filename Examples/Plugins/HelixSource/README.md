# Helix plugin

This XML only plugin demonstrates how to provide a Programmable Filter
with dedicated Property to control some Python variables.

A variable can be controlled through a Property with the following design:
- the command should be `SetParameter`.
- the attribute `initial_string` should be set to the variable name.
