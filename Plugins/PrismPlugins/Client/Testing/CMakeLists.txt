INCLUDE(ParaViewTestingMacros)

SET (PRISM_TESTS_WITH_BASELINES
  ${CMAKE_CURRENT_SOURCE_DIR}/Prism1.xml
)

SET (Prism1_BREAK TRUE)

IF (PARAVIEW_DATA_ROOT)
  add_client_tests("Prism"
    BASELINE_DIR ${PARAVIEW_DATA_ROOT}/Baseline
    TEST_SCRIPTS ${PRISM_TESTS_WITH_BASELINES}
  )

#  add_client_server_tests("PrismCS"
#    BASELINE_DIR ${PARAVIEW_DATA_ROOT}/Baseline
#    TEST_SCRIPTS ${PRISM_TESTS_WITH_BASELINES}
#  )

#  IF (PARAVIEW_CLIENT_RENDER_SERVER_TESTS)
#    add_client_render_server_tests("PrismCRS"
#      BASELINE_DIR ${PARAVIEW_DATA_ROOT}/Baseline
#      TEST_SCRIPTS ${PRISM_TESTS_WITH_BASELINES}
#    )
#  ENDIF (PARAVIEW_CLIENT_RENDER_SERVER_TESTS)


ENDIF (PARAVIEW_DATA_ROOT)
