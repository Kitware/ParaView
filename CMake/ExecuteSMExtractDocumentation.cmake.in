
if(WIN32)
  get_filename_component(QT_BIN_DIR "@QT_QMAKE_EXECUTABLE@" PATH)
  get_filename_component(PYTHON_BIN "@PYTHON_EXECUTABLE@" PATH)
  set(ENV{PATH} "${PYTHON_BIN};${QT_BIN_DIR};$ENV{PATH}")
elseif(APPLE)
  set(ENV{DYLD_LIBRARY_PATH} "@QT_LIBRARY_DIR@:$ENV{DYLD_LIBRARY_PATH}")
else(WIN32)
  get_filename_component(PYTHON_LIB "@PYTHON_LIBRARY@" PATH)
  set(ENV{LD_LIBRARY_PATH} "${PYTHON_LIB};@QT_LIBRARY_DIR@:$ENV{LD_LIBRARY_PATH}")
endif(WIN32)

execute_process(
  COMMAND "@ParaView_BINARY_DIR@/bin/${OutDir}/vtkSMExtractDocumentation" "@ParaView_BINARY_DIR@/Documentation" "${fullXMLFile}"
  )
