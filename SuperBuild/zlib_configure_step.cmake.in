
if(APPLE)
  set(ENV{CPPFLAGS} "@pv_tpl_cxx_flags@ -arch x86_64 -mmacosx-version-min=@CMAKE_OSX_DEPLOYMENT_TARGET@ -isysroot @CMAKE_OSX_SYSROOT@")
  set(ENV{CXXFLAGS} "@pv_tpl_cxx_flags@ -arch x86_64 -mmacosx-version-min=@CMAKE_OSX_DEPLOYMENT_TARGET@ -isysroot  @CMAKE_OSX_SYSROOT@")
  set(ENV{CFLAGS} "@pv_tpl_c_flags@ -arch x86_64 -mmacosx-version-min=@CMAKE_OSX_DEPLOYMENT_TARGET@ -isysroot  @CMAKE_OSX_SYSROOT@")
endif()

EXECUTE_PROCESS(
  COMMAND sh configure --prefix="@zlib_install@"
  WORKING_DIRECTORY "@zlib_source@"
  )
