## Select arrays in the VTK.JS exporter

You can now choose the arrays exported for each source dataset in the VTK.JS and Glance exporter. This enables lighter exports containing only the required datasets.
