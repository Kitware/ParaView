## Support jpeg extension for texture file

The jpeg extension is now supported for texture files.
The texture combo box dialog filters the `*.jpeg` filenames and the `vtkNetworkImageSource` is able to handle it.
