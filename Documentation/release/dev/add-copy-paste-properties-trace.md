# Add copy-paste properties in Python Trace

- The objects are now correctly updated when using `Copy` function with Python scripting
- The use of __Copy/Paste__ buttons from the __Properties Panel__ is now included in the Python Trace
