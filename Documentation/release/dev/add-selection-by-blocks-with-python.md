# Add selection by block with Python

It is now possible to select points/cells from a block of a composite dataset with Python with the function `SelectBlocks(BlockSelector, Source FieldType, Modifier)`
