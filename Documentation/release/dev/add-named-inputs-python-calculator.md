## Add the possibility to use named inputs in **Python Calculator**

It is now possible to use the names from the __Pipeline Browser__ (aka "RegisteredNames") to retrieve the inputs instead of using their index in the **Expression** of **Python Calculator**, for example : `Sphere2` or `inputs["Sphere2"]` instead of using `inputs[1]`
