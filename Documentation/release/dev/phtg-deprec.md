## Parallel XML HyperTreeGrid (.phtg) writer and reader deprecated

The .phtg format had issues and inconsistent results in parallel,
the writer, reader & extractor are now deprecated.

Users should use the VTKHDF format instead, which supports HyperTreeGrid in parallel properly.
