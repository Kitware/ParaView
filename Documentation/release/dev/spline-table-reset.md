## Replace remove all points by reset points in Spline table

The "Remove all points" button in the spline points table has been replaced by "Reset Points"
Clicking this button now brings back the 2 default points instead of removing all the points.
