# Rename VR Plugin to CAVE Interaction

The ParaView plugin `VRPlugin` has been renamed to `CAVEInteraction`. As the new name implies, this plugin is used to
interact with Cave Automatic Virtual Environments.
