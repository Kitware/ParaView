## Generify Ghost Cells Generator

You can now use the `Ghost Cells Generator` without worrying
about the input type. It now also supports Hyper Tree Grid.

The HTG specific one remains visible in ParaView.
