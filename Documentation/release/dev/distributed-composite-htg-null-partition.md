## Ghost Cells Generator now supports null partition on distributed composite htg

In a distributed context, the ghost cells generator now supports having a process that has a null partition/block
when other have HTGs.
