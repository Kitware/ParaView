# Add Edge Opacity Control

A new property has been added to control the opacity of edge in `Surface with edges` representation.

![Illustration of using the Edge opacity property](add-edge-opacity-control.gif)
