## Avoid overlapping labels in the color legend

In some cases, labels could become illegible due to overlap. Drawing overlapping labels is now avoided by default,
but can be enabled via the `AllowOverlappingLabels` property in the color legend editor to match previous behavior.
