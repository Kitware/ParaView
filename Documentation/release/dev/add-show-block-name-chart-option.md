## Add Show Block Name option in Chart View for composite dataset

It is now possible to hide block name in a _Chart View_ for composite dataset with the _Individual Block_ array option by unchecking the _Show Block Name_ checkbox (checked by default).
