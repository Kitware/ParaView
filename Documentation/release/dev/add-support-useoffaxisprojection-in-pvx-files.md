## Add support for UseOffAxisProjection configuration in .pvx files

The PVX configuration file for CAVE now support the `UseOffAxisProjection` parameter for the camera.
It is enabled by default and it can be disabled with `<UseOffAxisProjection Value="false" />`
