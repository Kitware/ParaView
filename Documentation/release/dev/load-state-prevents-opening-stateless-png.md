## "Load State" dialog does not allow selecting stateless PNG files.

PNG screenshots with state metadata can be opened in the "Load State" dialog. PNG files without state can no longer be
selected in that dialog.
