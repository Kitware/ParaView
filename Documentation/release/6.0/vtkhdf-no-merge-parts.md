## VTKHDF Reader: remove "Merge Parts" option

The "Merge Parts" option was removed from the VTKHDF Reader. Users should manually create a "MergeBlocks" filter if a non-partitioned data type is required.
