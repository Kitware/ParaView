SET(VTK_KITS_DIR "${XDMF_KITS_DIR}")
INCLUDE_DIRECTORIES(${XDMF_INCLUDE_DIRS})
IF (VTK_USE_X)
  INCLUDE_DIRECTORIES(${X11_X11_INCLUDE_PATH})
ENDIF (VTK_USE_X)
SET(KIT_WRAP_DEPS RenderingOpenGL)
PV_WRAP_VTK_CS(Xdmf XDMF "${KIT_WRAP_DEPS}")
