# This causes the composite to use char instead of floats for RGB.
# It will cause the composite delay to be smaller, but may introduce
# quantization artifacts into the rendering.

Application BroadcastSimpleScript {TreeComp1 UseCharOn}
