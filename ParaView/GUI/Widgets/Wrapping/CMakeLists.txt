# Handle the subdirectories

SUBDIRS(Tcl)

IF(VTK_WRAP_PYTHON)
  SUBDIRS(Python)
ENDIF(VTK_WRAP_PYTHON)

# Generate default setup paths

IF(KWWidgets_USE_SOVIEWER AND SOV_FOUND)
  INCLUDE(${SOV_USE_FILE})
ENDIF(KWWidgets_USE_SOVIEWER AND SOV_FOUND)

INCLUDE("${KWWidgets_CMAKE_DIR}/KWWidgetsPathsMacros.cmake")
KWWidgets_GENERATE_DEFAULT_SETUP_PATHS("${EXECUTABLE_OUTPUT_PATH}")
